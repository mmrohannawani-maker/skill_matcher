# Core FastAPI dependencies - version locked for stability
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
pydantic==2.5.0
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.6
python-dotenv==1.0.0
email-validator==2.1.0

# Data science & ML dependencies - with numpy pinned to compatible version
pandas==2.0.3
openpyxl==3.1.2
numpy==1.24.3  # CRITICAL: This version works with Python 3.9 on Linux
scikit-learn==1.3.0

# NLP dependencies - downgraded spaCy for better compatibility
spacy==3.7.2  # Older but stable version for Python 3.9
thinc==8.2.3  # Compatible with spaCy 3.7.2
blis==1.2.0   # Compatible with thinc 8.2.3
catalogue==2.0.10
typing-extensions==4.5.0

# Sentence transformers and dependencies
sentence-transformers==2.2.2
torch==2.0.1  # CPU version for Vercel (no GPU needed)
transformers==4.35.0
tokenizers==0.15.0
 huggingface-hub==0.19.4

# LangChain ecosystem
langchain==0.1.0
langchain-huggingface==0.0.3
huggingface_hub==0.19.4

# Vercel serverless compatibility
mangum==0.17.0